
{
  "name": "my-node-ec2-app",
  "version": "1.0.0",
  "description": "Simple Node.js app for AWS CodePipeline + CodeDeploy (EC2)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "build": "echo "No build step needed"",
    "test": "echo "No tests" && exit 0"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
